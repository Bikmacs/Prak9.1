import tv.Otparvka
import tv.Totovar
import tv.Tovar
import tv.Train
import javax.lang.model.util.Elements.Origin

fun main(){

    var Tovar1: Tovar = Tovar()
    Tovar1.input(Tovar1)
    Tovar1.output(Tovar1)

    var toTovar: Tovar = Tovar()
    Totovar.buy(toTovar)

    var Train1: Train = Train(0.0,"",0.0)
    Train1.input(Train1)
    Train1.vesTrain(Train1)
    Train1.peopl(Train1)
    Train1.gruzpod(Train1)

    var Otpravka1: Otparvka = Otparvka("",0.0,0.0,"",0.0)
    Otpravka1.speed(Otpravka1)




}
